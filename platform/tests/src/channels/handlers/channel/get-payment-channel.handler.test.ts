/**
 * GetPaymentChannel handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: channels
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getPaymentChannel } from "@hopsettle/api-server/domains/channels/handlers";
import type { ChannelsDDDDependencies } from "@hopsettle/api-server/domains/channels";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@hopsettle/tests/shared";
import { CHANNELS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetPaymentChannel", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ChannelsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ChannelsDDDDependencies>(
      CHANNELS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetPaymentChannel, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { channelId: entity.id };

    await getPaymentChannel(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetPaymentChannel.execute).toHaveBeenCalledWith(expect.objectContaining({ channelId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { channelId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetPaymentChannel, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getPaymentChannel(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
