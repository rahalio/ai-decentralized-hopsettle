/**
 * OfferRepository - DynamoDB Implementation (PauseOffer port backing)
 *
 * Generated by DynamoDB Repository Generator v2.0.0
 * Domain: staking
 */

import type { PauseOfferRepository } from "@hopsettle/services/staking";

export class OfferRepositoryDdb implements PauseOfferRepository {
  constructor(private readonly dynamoClient: any) {}

  async pauseNewChannelOffers(
    input: Parameters<PauseOfferRepository["pauseNewChannelOffers"]>[0]
  ): Promise<Awaited<ReturnType<PauseOfferRepository["pauseNewChannelOffers"]>>> {
    void this.dynamoClient;
    void input;
    // Sandbox stub — wire Dynamo when x-dynamodb metadata is added
    throw new Error("Not implemented - missing x-dynamodb metadata");
  }
}
