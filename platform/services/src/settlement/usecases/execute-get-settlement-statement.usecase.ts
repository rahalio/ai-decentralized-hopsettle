/**
 * ExecuteGetSettlementStatement - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: settlement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSettlementStatement Use Case
 *
 * DDD: Application use case for get Statement.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSettlementStatementInput, GetSettlementStatementOutput } from "../dto/statement.dto";
import type { ExecutionContextService, IdGeneratorService } from "@hopsettle/services/_shared/index.js";
import type { StatementRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSettlementStatement {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly statement: StatementRepository
  ) {}

  async execute(input: GetSettlementStatementInput): Promise<GetSettlementStatementOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.stlId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).statementId) {
      throw new ValidationError("statementId is required");
    }
    const statementId = (input as any).statementId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.statement.getSettlementStatement({ statementId: statementId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${statementId}`);
    }

    return entity as GetSettlementStatementOutput;
  }
}
