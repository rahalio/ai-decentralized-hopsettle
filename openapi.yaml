# Hopsettle OpenAPI — source of truth moved to domain specs.
#
# Canonical contracts live under:
#   packages/openapi-core/src/{domain}.yaml
#   packages/openapi-core/src/{domain}.schemas.yaml
#   packages/openapi-core/src/common/
#
# Domains: identity, channels, accounting, pricing, staking,
#          settlement, reporting, disputes, topology
#
# Bundle: pnpm bundle:openapi
# Lint:   pnpm lint:openapi
